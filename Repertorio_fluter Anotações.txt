#compila e roda no terminal
flutter run 

#compilar via wifi
adb connect 192.168.15.14:46839 

flutter clean
flutter pub get


flutter run -d 192.168.15.14:46839

flutter clean
flutter pub get

flutter build apk --release --split-per-abi


#Github
# clonar um repo do GitHub
git clone https://github.com/usuario/nome-projeto.git

# entrar na pasta
cd nome-projeto

# ver o que foi alterado, adicionado, etc.
git status

# ver o log de commits
git log --oneline

git add .
git commit -m "uma merda"
git push origin main

# enviar para o repositório remoto
git push origin main


# pra novo splash screen nativo
flutter pub get
dart run flutter_native_splash:create



git add .
git commit -m "Pasta pode ser excluída e arquivo também"
git push origin main


# Gera aab
flutter build appbundle --release

powershell
flutter build apk --release
Ou, se quiser APKs menores por arquitetura (recomendado para testes mais “reais”):

powershell
flutter clean
flutter pub get
flutter build apk --release --split-per-abi
flutter run -d windows


flutter clean
flutter pub get
flutter run -d windows

#Run no chrome
flutter run -d chrome

Ambiente android para m10A - tablet velho
flutter clean
flutter pub get
- flutter run -d 001200629033611 

# Tablet 
flutter clean
flutter pub get
- flutter run -d 001200629033611 

s dois melhores links fáceis de lembrar para ver sempre os emojis mais atuais são:

Lista oficial mais recente em português (Unicode 16.0) na Emojipedia:
https://emojipedia.org/pt/unicode-16.0

Site com todos os emojis atualizados (inclui novos de 2026), com busca e categorias:
https://emojiterra.com


Ícones
https://cupertino-icons.web.app
https://fonts.google.com/icon

flutter clean
flutter pub get

flutter create . --platforms=windows
flutter run -d windows

# Mudando icone
flutter pub get
flutter pub run flutter_launcher_icons

#Mudando splash screen
flutter pub get
flutter pub run flutter_native_splash:create
